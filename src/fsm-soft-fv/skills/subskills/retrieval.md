# Retrieval Subskill

1. Build query plans with the LLM retrieval planning subskill.
2. For each query, pick retrieval tool (`search`, `web_search`) and execute.
3. Optionally enrich result URLs with `page_fetch` and `sentence_extract`.
4. Convert to canonical evidence items.
